# H200 按键事件回传

应用启动同步成功后，会继续持有 H200 协议 HID 接口，并注册输入报告回调。物理按键事件通过同一个连接回传到应用内状态，效果等同于在界面上鼠标点击对应格子。

## 输入包格式

当前只解析参考仓库确认的 `0x0101` 按键输入包：

```text
[0x7c 0x7c][cmd:0x0101 u16 BE][length u32 LE][state u8][index u8][type u8][action u8]
```

字段约定：

- `index`：LCD 格子从 `0` 开始编号，当前 H200 原型映射到应用内按键 `1...14`。
- `type == 0x02`：旋钮事件；其他值按普通按键处理。
- `action == 0x01`：按下；`0x02` 和 `0x03` 分别表示旋钮左转和右转；其他值按释放处理。

## 当前行为

- 只响应 `type == button` 且 `action == press` 的事件。
- `index 0...13` 映射到应用内 `1...14`，并调用与鼠标点击相同的 `pressKey` 入口。
- 释放事件不会重复增加点击次数。
- 旋钮、页面键和未知索引暂不触发 14 格原型 UI。

当前点击只更新应用内交互状态；按键图像仍按启动包同步逻辑发送，后续如果要让点击次数也实时回写到设备屏幕，应在同一个状态入口后增加完整包或局部更新包发送。
