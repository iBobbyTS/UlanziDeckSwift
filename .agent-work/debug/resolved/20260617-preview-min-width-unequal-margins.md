## 现象

最小窗口宽度时，预览区域四边距看起来不均等。

## 已确认

- 预览区本身的内容宽高已经有统一的度量。
- 当前窗口最小宽度用的是右栏最小宽度，而不是右栏的稳定展示宽度。
- 最小时右栏会被压到更紧，左侧预览列没有拿到稳定的最小展示空间。

## 当前假设

窗口最小时，应该按右栏的理想宽度计算最小窗口宽度，再让右栏在更大的窗口里做小范围弹性。

## 下一步

把最小窗口宽度改为：预览区最小宽度 + 分隔线 + 右栏理想宽度。
